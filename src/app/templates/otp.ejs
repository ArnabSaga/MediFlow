<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Email</title>
    <style>
        body {
            background-color: #F3F4F6;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .wrapper {
            width: 100%;
            table-layout: fixed;
            padding: 40px 0;
            background-color: #F3F4F6;
        }

        .card {
            max-width: 460px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08);
            border: 1px solid #f1f1f1;
        }

        h2 {
            color: #1F2937;
            font-size: 26px;
            font-weight: 700;
            margin: 0 0 12px 0;
            letter-spacing: -0.5px;
        }

        p {
            color: #6B7280;
            font-size: 15px;
            line-height: 1.6;
            margin: 0 0 24px 0;
        }

        /* --- OTP Code Box --- */
        .otp-wrapper {
            background: #F9FAFB;
            border: 2px dashed #E5E7EB;
            border-radius: 16px;
            padding: 24px;
            margin: 0 auto 30px auto;
            /* More space below */
        }

        .otp-code {
            font-family: 'Courier New', Courier, monospace;
            font-size: 36px;
            font-weight: 700;
            color: #111827;
            letter-spacing: 12px;
            margin-left: 12px;
            display: block;
        }

        /* --- STACKED TIMER STYLES --- */
        .timer-stack {
            display: block;
            width: 100%;
            max-width: 300px;
            /* Constrain width for better look */
            margin: 0 auto 32px auto;
            text-align: center;
        }

        /* The pulsing text */
        .timer-text {
            display: inline-block;
            font-size: 13px;
            font-weight: 700;
            color: #EF4444;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
            /* Spacing between text and bar */
            animation: pulse 1s infinite alternate;
            /* Makes it breathe */
        }

        /* Icon styling */
        .timer-icon {
            font-style: normal;
            margin-right: 4px;
        }

        /* The bar track */
        .progress-track {
            width: 100%;
            height: 8px;
            background-color: #F3F4F6;
            border-radius: 100px;
            overflow: hidden;
            position: relative;
        }

        /* The moving bar */
        .progress-bar {
            height: 100%;
            width: 100%;
            background: linear-gradient(90deg, #6366F1, #EF4444);
            /* Indigo to Red */
            border-radius: 100px;
            transform-origin: left;
            animation: countdown 120s linear forwards;
        }

        /* Animations */
        @keyframes countdown {
            0% {
                width: 100%;
            }

            100% {
                width: 0%;
            }
        }

        @keyframes pulse {
            0% {
                opacity: 1;
                transform: scale(1);
            }

            100% {
                opacity: 0.7;
                transform: scale(0.98);
            }
        }

        /* Footer */
        .divider {
            height: 1px;
            background-color: #F3F4F6;
            margin: 32px 0;
        }

        .footer-text {
            font-size: 12px;
            color: #9CA3AF;
            line-height: 1.5;
        }

        .secure-badge {
            display: inline-block;
            background: #ECFDF5;
            color: #059669;
            font-size: 11px;
            padding: 4px 8px;
            border-radius: 4px;
            font-weight: 600;
            margin-top: 10px;
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <div class="card">

            <h2>Verify your identity</h2>
            <p>Hi <%= name %>, please enter the following verification code to access your account.</p>

            <div class="otp-wrapper">
                <span class="otp-code">
                    <%= otp %>
                </span>
            </div>

            <div class="timer-stack">
                <div class="timer-text">
                    <span class="timer-icon">⏰</span> Expires in 2 minutes
                </div>
                <div class="progress-track">
                    <div class="progress-bar"></div>
                </div>
            </div>

            <p class="footer-text">
                <span class="secure-badge">Secure Verification</span>
                <br><br>
                If you did not request this code, please ignore this email.
            </p>

            <div class="divider"></div>

            <p class="footer-text">
                &copy; <%= new Date().getFullYear() %> Medi-Flow Inc.<br>
                    Khulna, Bangladesh.
            </p>
        </div>
    </div>
</body>

</html>
