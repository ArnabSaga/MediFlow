<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verification Code - MediFlow</title>
    <style>
        :root {
            --primary-color: #0ea5e9;
            --primary-dark: #0284c7;
            --secondary-color: #f0f9ff;
            --text-main: #1e293b;
            --text-muted: #64748b;
            --border-color: #e2e8f0;
            --bg-color: #f8fafc;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }

        .container {
            max-width: 600px;
            margin: 40px auto;
            background-color: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.05);
            border: 1px solid var(--border-color);
        }

        .header {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
            color: white;
            padding: 35px 40px;
            text-align: center;
        }

        .header-icon {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 64px;
            height: 64px;
            background-color: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            margin-bottom: 15px;
            font-size: 30px;
        }

        .header h1 {
            font-size: 26px;
            font-weight: 800;
            letter-spacing: -0.5px;
            margin-bottom: 5px;
        }

        .content {
            padding: 40px;
            text-align: center;
        }

        .greeting {
            font-size: 20px;
            color: var(--text-main);
            margin-bottom: 15px;
            font-weight: 600;
        }

        .instructions {
            color: var(--text-muted);
            font-size: 15px;
            margin-bottom: 30px;
            line-height: 1.6;
        }

        .otp-box {
            background-color: var(--secondary-color);
            border: 2px dashed var(--primary-color);
            border-radius: 12px;
            padding: 25px 20px;
            margin-bottom: 30px;
        }

        .otp-code {
            font-family: 'Courier New', Courier, monospace;
            font-size: 42px;
            font-weight: 800;
            color: var(--primary-dark);
            letter-spacing: 12px;
            margin-left: 12px;
            /* to offset the tracking from centering */
        }

        .timer-warning {
            display: inline-flex;
            align-items: center;
            background-color: #fee2e2;
            color: #b91c1c;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 13px;
            font-weight: 600;
            margin-bottom: 30px;
        }

        .security-notice {
            background-color: #fafafa;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 20px;
            text-align: left;
            font-size: 13px;
            color: var(--text-muted);
            border-left: 4px solid #f59e0b;
        }

        .footer {
            background-color: var(--text-main);
            color: white;
            padding: 30px 40px;
            text-align: center;
        }

        .footer-logo {
            font-size: 18px;
            font-weight: 700;
            margin-bottom: 15px;
        }

        .footer-text {
            color: #94a3b8;
            font-size: 12px;
            margin-bottom: 10px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="header-icon">🛡️</div>
            <h1>Verification Code</h1>
            <p style="opacity: 0.9; font-size: 15px;">Secure Account Access</p>
        </div>

        <div class="content">
            <div class="greeting">Hello <%= name %>,</div>
            <p class="instructions">
                A request has been received to verify your MediFlow account. Please use the following One-Time Password
                (OTP) to complete the verification process.
            </p>

            <div class="otp-box">
                <div class="otp-code">
                    <%= otp %>
                </div>
            </div>

            <div class="timer-warning">
                ⏳ This code will expire in 2 minutes
            </div>

            <div class="security-notice">
                <strong style="color: #b45309;">Security Alert:</strong>
                <p style="margin-top: 5px;">Never share this code with anyone. Our support team will never ask for your
                    OTP. If you did not request this verification, please secure your account immediately or ignore this
                    email.</p>
            </div>
        </div>

        <div class="footer">
            <div class="footer-logo">MediFlow Health Systems</div>
            <div class="footer-text">
                &copy; <%= new Date().getFullYear() %> MediFlow Inc., Khulna, Bangladesh.<br>All rights reserved.
            </div>
        </div>
    </div>
</body>

</html>
